from setuptools import setup, find_packages

setup(
    name='etd_new',
    version='0.1.0',
    description='A new ETD (Electronic Theses and Dissertations) processing package',
    author='Your Name',
    author_email='your.email@example.com',
    packages=find_packages(),
    install_requires=[
        # Add your dependencies here, e.g. 'numpy', 'pandas'
    ],
    python_requires='>=3.7',
)